# PasteLink 项目开发与质量保障规则

## 核心开发与交付规范

每次对项目代码进行新增或修改后，**必须严格执行以下三步闭环流程**：

### 1. 代码合规性与边界静态检查 (Self-Check)
* **SDK 与环境沙盒检查**：
  * 若修改 iOS 代码，必须严格核查 Target 编译范围（Main App vs Widget / Extension），严禁在 Extension 相关文件中调用 `UIApplication.shared` 等受限 API；
  * 核查 `AppIntent` 与 `LiveActivityIntent`、`ControlWidget` 等协议签名与参数标签（如 `ControlWidgetButton(action:)`、`Button(intent:)`）；
* **跨平台协议对齐**：核查 BLE UUID、特征值定义、数据包大小限制（16KB）以及序列化格式在 Windows (Rust) 与 iOS (Swift) 两端的一致性；
* **引用与命名规范**：确保所有符号均在作用域内，不存在悬挂引用、未导入模块或废弃 API。

### 2. 自动化验证与单元测试 (Testing)
* **环境可行性检查**：检查当前执行环境是否具备运行测试的工具链（如 `cargo test`、`swift test`、`xcodebuild test` 等）；
* **测试执行**：
  * 在支持的环境下（如 Windows 端 Rust 工具链具备时），必须执行单元测试命令验证逻辑完整性；
  * 若当前宿主环境无法直接运行特定平台的测试套件（例如在 Windows 宿主环境下无法直接调用 `xcodebuild` 跑 iOS 模拟器测试），需如实说明并完成全面的静态与逻辑推演分析。

### 3. 交付质检报告 (Verification Report)
每次完成代码修改后，在向用户汇报时，**必须提供结构化的《代码变更与质检报告》**，报告应包含：
* **变更范围与目标**：明确修改了哪些文件、解决了什么具体问题；
* **关键改动与技术细节**：说明核心逻辑与 API 调用；
* **自检与静态分析结果**：列出针对 Target 隔离、沙盒权限、协议签名等进行的检查项与结论；
* **测试执行状态**：明确汇报单元测试运行结果，或说明当前环境为何跳过测试及静态验证结论。

### 4. 需求批判性辨别与事实证据链 (Critical Verification & Evidence)
* **独立判断与有效性审核**：严禁盲从用户输入。针对用户提出的指令、技术假设或架构方案，必须主动进行技术可行性、平台边界（如 iOS 沙盒、后台保活、Windows BLE 特征等）与逻辑自洽性评估；若存在不合理或错误，必须明确指出并给出替代方案与充分理由；
* **前置自检与证据支撑**：在交付任何结论、配置或代码前，必须完成深度自我推演与正确性检验，确保方案真实可用；交付时必须提供充分的客观证据（如官方规范、API 限制说明、测试验证日志或静态推导链）。

